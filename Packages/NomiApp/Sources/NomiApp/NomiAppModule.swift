import NomiCore
import NomiIngest
import NomiUI

/// Version stub. Composition root — wires real services to NomiUI — lands here in U8.
public enum NomiAppModule {
  public static let version = "0.1.0"
}
